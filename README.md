# SecPass

Менеджер паролей с шифрованием AES-256-CBC/ChaCha20, защитой памяти и графическим интерфейсом.

## Возможности

- **Шифрование**: AES-256-CBC + ChaCha20, ключи выводятся через Argon2id
- **Двухслойное шифрование**: тела записей шифруются дважды
- **Защита памяти**: SecureString с mlock/VirtualLock (без свопинга на диск)
- **Ключ-файл**: опциональный второй фактор при разблокировке
- **Группы**: древовидная структура, перетаскивание записей
- **Генератор паролей**: со встроенным расчётом энтропии
- **Вкладки**: работа с несколькими хранилищами одновременно
- **Поиск**: фильтрация записей по имени и URL

## Установка

```bash
git clone <repo>
cd secpass
python -m venv .venv
.venv\scripts\activate
pip install PySide6>=6.5.0 cryptography>=41.0.0 argon2-cffi>=23.1.0
```

## Запуск

```bash
python main.py
```

Отладка: `python main.py --debug`

## Структура проекта

```
secpass/
├── crypto/          # AES-CBC, ChaCha20, Argon2id
├── secure/          # SecureString (mlock, zeroize)
├── storage/         # Хранилище: записи, группы, masterkey
├── gui/             # Интерфейс на PySide6
│   ├── dialogs/     # Диалоги создания, входа
│   ├── views/       # Qt-виджеты (деревья, строки)
│   └── helpers/     # Утилиты паролей
├── log.py           # NullHandler-логгер (debug по флагу)
└── main.py          # Точка входа
```

## Криптография

```
Пароль ───────────────────────────────────────┐
Ключ-файл (опционально) ──────────────────────┤
                                               ▼
                              Argon2id (salt 16 сл. байт)
                               memory=19 MiB, iterations=2,
                               parallel=1, tag=32 байта
                                    │
                                    ▼
                              master_key (32 байта)
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
            AES-256-CBC (PKCS7)            AES-256-CBC (PKCS7)
            ключ: master_key               ключ: master_key
            ║                             ║
  ┌─────────────────────┐       ┌─────────────────────┐
  │ head записи/группы  │       │ 1-й слой: body      │
  │ (SHA256(UUID).head) │       │ (метаданные)        │
  └─────────────────────┘       │ AES-256-CBC         │
                                │ 2-й слой: body      │
                                │ (пароль/заметки)    │
                                │ AES-256-CBC         │
                                │ (SHA256(UUID).body) │
                                └─────────────────────┘

Имена файлов:      SHA256(UUID) — без утечки содержимого
Соль master-ключа: CSPRNG, 16 байт
IV:                CSPRNG, 16 байт (новый на каждое шифрование)
```

## Безопасность

- Минимальная энтропия пароля: **80 бит**
- Пароли в памяти защищены `VirtualLock`/`mlock`
- При блокировке хранилища данные зануляются (`zeroize`)
- Имена файлов — SHA256(UUID), без утечки содержимого

## Системные требования

- Python 3.14+
- Windows / Linux / macOS

## О программе

- Версия: 1.0.0
- Разработчик: Vladislav Panov
- Контакты: abasecode@gmail.com
